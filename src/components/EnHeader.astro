---
import LanguageSwitcher from "./LanguageSwitcher.astro";
const links = [
  { href: "/en/publications", label: "Publications" },
  { href: "/en/education", label: "Education" },
  { href: "/en/experiences", label: "Experiences" },
  { href: "/en/awards_and_grants", label: "Awards and Grants" },
];
---
<header class="navbar">
  <nav>
    <div class="navbar__inner">
      <!-- logo -->
      <div class="navbar__logo">
        <a href="/en/">Shuichiro Shimizu</a>
      </div>

      <!-- desktop links -->
      <ul class="navbar__list">
        {links.map(link => (
          <li><a href={link.href}>{link.label}</a></li>
        ))}
        <LanguageSwitcher />
      </ul>

      <!-- mobile toggle -->
      <button id="menu-button" class="navbar__toggle">☰</button>
    </div>

    <!-- mobile dropdown -->
    <div id="mobile-menu" class="navbar__mobile hidden">
      {links.map(link => (
        <a href={link.href}>{link.label}</a>
      ))}
    </div>
  </nav>
</header>

<script>
  document.getElementById('menu-button')
    .addEventListener('click', () =>
      document.getElementById('mobile-menu').classList.toggle('hidden')
    );
</script>
